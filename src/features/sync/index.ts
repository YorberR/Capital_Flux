export { syncEngine, SyncEngine } from './engine';
export { resolveConflict, detectConflict } from './conflict-resolver';
export type {
  SyncEntityType,
  SyncOperation,
  SyncResult,
  ConflictResolution,
  SyncStats,
  SyncConfig,
  SyncEventHandlers,
  DEFAULT_SYNC_CONFIG,
} from './types';
